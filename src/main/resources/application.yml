#spring:
#  application:
#    name: BodyMeasurementApplication

#server:
#  port: 8080
#  servlet:
#    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_body_measurement_db?createDatabaseIfNotExist=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

#  # JPA / Hibernate
#  jpa:
#    hibernate:
#      ddl-auto: create
#    show-sql: false
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
#        format_sql: true




app:
  # File Storage
  file:
    upload-dir: uploads/images
    base-url: http://localhost:8080/api/images

#  # JWT
#  jwt:
#    secret: your-super-secret-jwt-key-change-this-in-production-minimum-256-bits
#    expiration: 86400000

  # ============================================================
  #  GOOGLE GEMINI AI - FREE (No credit card needed!)
  #  Get API Key here: https://aistudio.google.com/app/apikey
  #  Free limits: 1500 requests/day
  # ============================================================
  gemini:
    api-key: AIzaSyBHzvegQUyjg0ejG63D6uMkKjC6LIuo3Og
    model: gemini-3-flash-preview

  # CORS
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173

# Logging
logging:
  level:
    com.bodymeasure: DEBUG
    org.springframework.security: WARN
    org.flywaydb: DEBUG